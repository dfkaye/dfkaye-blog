import { model } from "../sam/model.js"
import { define } from "/js/lib/sam/define.js"

describe("model", () => {
  var { expect } = chai

  it("runs", () => {
    expect(expect).to.be.a("function")
  })
})
